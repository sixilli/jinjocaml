(* Schema for code to be created *)

(*
   Steps below should be completed for each parsed template

type foo_ctx = { bar : string; baz : bool; hi : string list }

let render_foo (ctx: foo_context) : string 
  create new map from foo_ctx
*)

(* open Base *)
(* open Ppxlib *)
(**)
(* let generate_ctx = () *)
let generate_renderer = ()
(*let ctx_generator = Deriving.Generator.V2.make_noarg generate_ctx *)
(* let render_generator = Deriving.Generator.V2.make_noarg generate_renderer *)

(* let deriver = *)
(*   Deriving.add *)
(*     "load_templates" *)
(*     ~str_type_decl:ctx_generator *)
(*     ~sig_type_decl:render_generator *)
(*     ~extension *)
(* ;; *)
